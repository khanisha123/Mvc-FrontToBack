@using FrontToBack.ViewModels
@model Product
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

@{
    ViewData["Title"] = "Index";
}






<table class="table table-bordered">
    <tr>
        <th>
            Image
        </th>
        @{

            var count = 0;

        }
        @*@foreach (var item in Model.commentList)
        {
            var txt1 = @item.Text;

            if (txt1.Length > 1)
            {
                count++;
            }
            var bi = "";
            bi = count.ToString()+" "+ "Comment";
            <th>
                @bi
            </th>
        }*@
       
    </tr>
    <tr>
        <td>
            <img style="width:200px;height:200px;" src="~/img/@Model.ImageUrl" />
        </td>
        @*@foreach (var item in Model.commentList)
        {
             <td> 

                     <span style="margin-right:10px;">@item.Text</span>
                     <a class="btn btn-info" asp-controller="ProductList" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                     <a class="btn btn-dark" asp-controller="ProductList" asp-action="Update" >Update</a>

             </td>
        }*@
        
    </tr>
</table>

       


<a asp-action="Create" class="btn btn-info">Comment</a>



@*@foreach (var item in Model.Comment)
    {
        <p>@item.Text</p>

    }*@


